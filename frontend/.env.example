# Example environment variables
# Use full redis URL when deploying to production, e.g. redis://:password@host:6379
REDIS_URL=

# When true, middleware will enforce Content-Security-Policy (no unsafe-inline/eval)
# and inject a per-request nonce. Requires you to add the nonce to any inline scripts.
CSP_STRICT=false

# Private RPC for server reads
BDAG_RPC=
NEXT_PUBLIC_PROJECT_ID=YOUR-WALLETCONNECT-PROJECT-ID;